extends: default

ignore:
  - tests/snapshots/
  - .claude/skills/automate-github-issues/assets/

rules:
  line-length:
    max: 120
  truthy:
    check-keys: false
  comments:
    min-spaces-from-content: 1
  document-start: disable
