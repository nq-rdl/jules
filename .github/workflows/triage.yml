name: Triage

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened, ready_for_review]
permissions: {}

jobs:
  triage:
    uses: nq-rdl/.github/.github/workflows/auto-triage.yml@main
    with:
      domain: RSE
    secrets: inherit
