name: Notify Slack on review request

on:
  pull_request:
    types: [review_requested]
permissions: {}

jobs:
  notify:
    uses: nq-rdl/.github/.github/workflows/auto-slack-review.yml@main
    secrets: inherit
